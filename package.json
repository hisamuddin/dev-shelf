{
  "name": "devshelf",
  "private": true,
  "version": "0.1.0",
  "engines": { "node": ">=22.22.0", "npm": ">=10.0.0" },
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
